type Props = {
  visible: boolean
  onLoadMore?: () => void
  loading?: boolean
}

/**
 * Avviso sui record già caricati disattivato: non viene più mostrato in nessuna pagina.
 */
export default function PaginatedFilterHint(_props: Props) {
  return null
}
